[
  {
    "id": "PROMPT-001",
    "enabled": true,
    "description": "Secret or credential pattern found in workspace prompt files",
    "tags": ["secrets", "prompts"],
    "target": { "type": "text", "path": null },
    "probe": {
      "type": "regex_any",
      "patterns": [
        "-----BEGIN (RSA|EC|OPENSSH) PRIVATE KEY-----",
        "(?i)(api[_-]?key|api[_-]?token|secret[_-]?key)\\s*[=:]\\s*['\"]?\\w{16,}",
        "(?i)bearer\\s+[a-zA-Z0-9\\-._~+/]+=*",
        "AKIA[0-9A-Z]{16}"
      ]
    },
    "condition": { "operator": "equals", "value": true },
    "finding": {
      "severity": "critical",
      "category": "secrets",
      "title": "Credential pattern in workspace prompt file",
      "detail": "A secret or API key pattern was detected in a workspace prompt file (SOUL.md, AGENTS.md, etc.). This will be sent to the AI model in every request.",
      "remediation": "Remove all credentials from prompt files. Use environment variables or ~/.openclaw/secrets/.env instead."
    }
  }
]
